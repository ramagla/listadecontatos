import { styled } from 'styled-components'
import variables from '../../globalStyles/variables'

export const BrandingStyle = styled.p`
  color: ${variables.brandingColor};
  padding: 15px;
  font-size: 20px;
  margin-left: 15px;
`

export const StatementStyle = styled.p`
  color: ${variables.sidebarColor};
  padding: 10px;
  font-size: 30px;
  text-align: center;
  font-weight: bold;
`
